$("textarea").val("");
$("#comment_<%= @invite.id %>").html("<%= j(render 'invites/comment', invite_comments: @invite.invite_comments) %>");
<% flash.each do |key, message| %>
$("#flash_messages").html("<div class='notification is-<%= key %>'><%= message %></div>");
<% end %>