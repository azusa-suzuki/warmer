<span class="devise-header">　会員の方はこちらからログイン　</span>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
   <br>
  <div class="field row">
    <label class="col-xs-2 col-xs-offset-1">メールアドレス</label>
    <div class="col-xs-6"><%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %></div>
  </div>
  <br>
  <div class="field row">
    <label class="col-xs-2 col-xs-offset-1">パスワード</label>
    <div class="col-xs-6"><%= f.password_field :password, autocomplete: "current-password", class: "form-control" %></div>
  </div>

  <%= link_to "=>パスワードを忘れた方はこちら", new_password_path(resource_name), class: "col-xs-offset-7" %>

  <% if devise_mapping.rememberable? %>
    <div class="field col-xs-offset-7">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit "ログイン", class: "btn btn-primary col-xs-offset-3" %>
  </div>
<% end %>

  <br>
<span class="devise-header">　登録がお済みでない方　</span>
<div class= "row">
  <br>
  <div class="col-xs-offset-2"><%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。</div>
</div>
